#include <iostream>

int width = 30;
int height = 12;
void RasterizationTriangle(float a[], int num){
    
    for (int i = 0; i < height; i++){
        for (int j = 0; j < width; j++){
            if ()
        }
    }
}

int main(){
    float a[] = {0.1, 0.1, 0.5, 1.0, 1.0, 0.5};
    RasterizationTriangle(a, 6);
}